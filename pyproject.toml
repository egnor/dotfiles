[project]
authors = [{ name = "Dan Egnor", email = "egnor@ofb.net" }]
dependencies = ["pyinfra>=3.8.0"]
name = "egnor-dotfiles"
requires-python = ">=3.12"
version = "0.0"

[tool.ruff]
line-length = 80
