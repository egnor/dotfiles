# Managed by pyinfra. Source: dotfiles/netdata/files/child/stream.conf
# Stream all metrics up to the parent (egnor-2020 / eacs.io). The API key
# matches the receive block in the parent's stream.conf.

[stream]
	enabled = yes
	destination = eacs.io:19999
	api key = 863719f0-df4b-54a5-9cf2-9641e21db01d
