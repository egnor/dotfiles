# Managed by pyinfra. Source: dotfiles/netdata/files/parent/stream.conf
# Top of the streaming tree: this node has no parent of its own, so
# [stream] enabled=no. The API-key block below is the *receive* side —
# children present this key to push metrics here. `health enabled = yes`
# (not `auto`) makes the parent always evaluate alerts for received
# metrics, since children are configured with [health] enabled=no.

[stream]
	enabled = no

# uuidgen -n @oid -s -N "quite"
[863719f0-df4b-54a5-9cf2-9641e21db01d]
	type = api
	enabled = yes
	allow from = *
	db = dbengine
	health enabled = yes
	replication period = 30d
