# Managed by pyinfra. Source: dotfiles/netdata/files.parent/go.d/x509check.conf
# TLS cert expiry monitoring. Stock alert template
# /usr/lib/netdata/conf.d/health.d/x509check.conf warns at <14d, crit at
# <7d. Source must be host:port; one job per (host, port). Same cert
# covers all paths on a host, so we don't need separate entries per URL.

jobs:
  - name: blackletterlabs
    source: https://blackletterlabs.com:443
  - name: shop_seventeengames
    source: https://shop.seventeengames.com:443
  - name: seventeengames
    source: https://seventeengames.com:443
  - name: 17g_io
    source: https://17g.io:443
