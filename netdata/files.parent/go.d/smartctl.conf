# Managed by pyinfra. Source: dotfiles/netdata/files.parent/go.d/smartctl.conf
# SMART collector on the parent too, for uniformity / future-proofing. The
# current parent (egnor-2020) is a GCE VM whose virtual disks expose no
# SMART data, so this finds no devices and idles — harmless. A bare-metal
# parent would collect real data. See files.child/go.d/smartctl.conf.

jobs:
  - name: smartctl
