# Managed by pyinfra. Source: dotfiles/netdata/files/child/netdata.conf
#
# Child node: ship metrics to the parent (egnor-2020 / eacs.io), no alerting.

[global]
	is ephemeral node = yes

[health]
	enabled = no
